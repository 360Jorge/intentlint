# intentlint
IntentLint is a pre-code-generation specification oversight tool. It checks whether a natural-language software spec is clear enough to safely implement, flagging ambiguity, missing constraints, hidden assumptions, contradictions, and edge cases.
